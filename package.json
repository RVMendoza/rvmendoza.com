{
  "name": "rvmendoza.com",
  "scripts": {
    "start": "node index.js",
    "sync": "browser-sync start --config bs-config.js",
    "deploy": "node index.js && rm -rf ./build/templates && surge --project ./build --domain rvmendoza.com"
  },
  "devDependencies": {
    "handlebars": "^3.0.1",
    "metalsmith": "^1.5.0",
    "metalsmith-markdown": "^0.2.1",
    "metalsmith-templates": "^0.7.0"
  }
}
